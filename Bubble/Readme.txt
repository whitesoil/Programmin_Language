1. Bubble sort 과제
- Bubble sort 까지만 구현하였습니다.
- Edit Configuration에 세팅 필요
- Edit Configuration에 Input값을 넣어주어야 한다.
- 모든 Input은 "" 로 감싸져야 한다.
- 입력 : 아래 예제 에서 "var x"의 배열변경 및 배열 크기에 맞추어 "var tempcount" 의 값을 변경하여준다.

Ex1) "var x = [6,8,9,7,5,4,2,3,1] ; var tempcount = 9; var hold; var loop; var i; while(loop < (tempcount-1)){while (i<(tempcount-(1+loop))){if(x[(i+1)] < x[i]){hold = x[i];x[i] = x[(i+1)];x[(i+1)] = hold else print 0}; i = (i+1)};i = 0; loop = (loop+1)}"
